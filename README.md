# PufferPanel on GitHub Codespaces

This repository starts PufferPanel inside Docker in a GitHub Codespace.

## Start
When the Codespace is created, it should auto-run the installer.

If needed, run manually:

```bash
chmod +x install-pufferpanel.sh
./install-pufferpanel.sh
